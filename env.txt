# =============================================================================
# VCPilot — Environment Configuration
# Copy this file to .env and fill in your values. Never commit .env to git.
# =============================================================================

# --- App ---
APP_ENV=production          # development | production
APP_SECRET_KEY=fe6ea24d13be3b9ba48d0a400868cc814006516a7a3e355899f8342f202e4f67
LOG_LEVEL=INFO

# --- PostgreSQL / TimescaleDB ---
POSTGRES_HOST=database
POSTGRES_PORT=5439
POSTGRES_DB=vcpilot
POSTGRES_USER=vcpilot
POSTGRES_PASSWORD=vcpilot_2026
DATABASE_URL=postgresql://vcpilot:vcpilot_2026@database:5432/vcpilot

# --- Redis (Celery broker + result backend) ---
REDIS_HOST=redis
REDIS_PORT=6389
REDIS_URL=redis://redis:6389/0

# --- IBKR Gateway ---
IBKR_HOST=ibkr
IBKR_PORT=4002             # 4001=live, 4002=paper
IBKR_CLIENT_ID=1
IBKR_ACCOUNT=U26352105              # Your IBKR account number
IBKR_PAPER_MODE=true       # true=paper, false=live — ALWAYS start paper
IBKR_SIMULATE=false        # true=simulate fills locally (sandbox), false=execute orders via IBKR gateway

# IBKR Gateway login (used by the gateway container)
IBKR_USERNAME=waglefamilytrust1
IBKR_PASSWORD=Praanu@22

# --- Data APIs ---
# yfinance is free/unlimited — no key needed
# FMP free tier: 250 calls/day — used only for supplementary fundamentals
FMP_API_KEY=8VAn0A08zjDIrY0J0NVKjobPyQ3yxPJU

# --- WAHA (WhatsApp HTTP API) ---
WAHA_HOST=whatsapp
WAHA_PORT=3000
WAHA_API_URL=http://whatsapp:3000
WAHA_API_KEY=vcpilot_2026
WAHA_DASHBOARD_PASSWORD=vcpilot_2026
WAHA_SESSION=default
# Webhook: WAHA posts incoming messages here → FastAPI processes them
WAHA_HOOK_URL=http://api:8501/webhook/whatsapp
# Your WhatsApp number in international format (no + or spaces)
WHATSAPP_ADMIN_NUMBER=61450325233
WHATSAPP_ADMIN_JID=61450325233@c.us

# --- Streamlit Dashboard ---
DASHBOARD_PORT=8501
DASHBOARD_PASSWORD=vcpilot_2026  # Simple password for the admin dashboard

# --- Super Admin Credentials ---
SUPERADMIN_EMAIL=anupam.wagle@gmail.com
SUPERADMIN_PASSWORD=vcpilot_2026

# --- Trading Settings (overridden by DB config, these are fallback defaults) ---
TRADING_UNIVERSE=ASX200       # ASX200 | ASX300 | ALLASX
BASE_CURRENCY=AUD
WEEKLY_CAPITAL_INJECTION=1000  # AUD added to account per week

# --- SMTP / Email Settings ---
# For unauthenticated local relay leave USERNAME/PASSWORD blank and SMTP_USE_TLS=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=shyftmate@gmail.com
SMTP_PASSWORD=hios wegc veuc jlog
SMTP_USE_TLS=true
SMTP_FROM_EMAIL=shyftmate@gmail.com
SMTP_FROM_NAME=VCPilot

# Set Telegram as the default notification channel for the platform
NOTIFICATION_CHANNEL=telegram

# Global Telegram Bot API configuration (obtain from @BotFather)
TELEGRAM_ENABLED=true
TELEGRAM_BOT_TOKEN=123456789:ABCdefGhIJKlmNoPQRsTUVwxyZ-example
TELEGRAM_CHAT_ID=-10023456789   # Target user or group chat ID
